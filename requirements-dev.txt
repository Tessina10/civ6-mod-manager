# Dépendances de dev uniquement. La dépendance runtime (PySide6) est dans
# requirements.txt à la racine.
# Installation : pip install -r requirements.txt -r requirements-dev.txt
pyinstaller
ruff
pytest
pre-commit
